import React from 'react';

const Treatement = (props) => {

    return (
      <div>
        <p>End time: {props.name}</p>
        <p>{props.name}</p>
        <p>{props.frequency}</p>
        <p>{props.name}</p>
    </div>
  )
}

export default Treatement 